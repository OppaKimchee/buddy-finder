
<% include partials/_header %>
<% include partials/_nav %>

<div class="container">
  <h3>Show all found pets</h3>
  <div class="row">
    <% pets.forEach(function(pet, index) { %>
      <% if (pet.category === 'Found') { %>
        <% include partials/_petdata %>
      <%}%>
    <% }) %>
  </div>
</div>

<% include partials/_footer %>
